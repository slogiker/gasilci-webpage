# PGD Majšperk Breg - Final Project Report

## Completed Phases:

### 1. Visual & Layout Refinements:
- **Full-Width Stretch**: Hero slider and Stats backgrounds now stretch edge-to-edge (100vw).
- **Reduced Height**: Stats bar and Footer heights reduced for a more compact, modern look.
- **Section Dividers**: Visibility increased (`rgba(255,255,255,0.15)`) between all content sections.
- **Navbar & Sidebar**: Hamburger moved to the right, sidebar slides from the right. Fixed mobile visibility logic.
- **Sidebar Links**: Fixed "Kontakt" anchor linking.

### 2. Login Page Redesign:
- **Improved UI**: Removed ugly gradients, used consistent dark background. Wider card (`500px`) for better balance.
- **Functional Upgrades**: 
    - Added support for login via **Email or Username**.
    - Specific error feedback: "Uporabnik ne obstaja" vs "Napačno geslo".
    - Password visibility toggle (`👁`) fixed and styled.
    - Consistent header and footer.

### 3. Admin Panel & JWT Fixes:
- **JWT Robustness**: Overhauled `get_auth_user()` in PHP to reliably detect `Authorization` headers across all environments.
- **Logout Bug**: Fixed the 401 response loop that was logging users out during edits. Improved `API.request` in JS with better logging and error catching.
- **Dashboard Upgrades**: 
    - Added a dedicated **Logout** button to the top-right header.
    - Improved modal scrolling for long forms.
    - Fixed category selection in gallery uploads.

### 4. Technical Infrastructure:
- **Placeholder Migration**: All image domains migrated to `placehold.co` to prevent SSL handshake errors.
- **Database Schema**: Added `username` column to `users` table and updated seeded users.
- **Docker Integration**: Correctly mapped volumes and verified API functionality inside the container.

## Current Project Structure:
- `public/`: Web application and Admin panel.
- `public/api/`: REST API with robust JWT authentication.
- `data/pgd.db`: SQLite database with seeded Slovenian content.

## Production Checklist (TODO):
- [ ] Replace `placehold.co` URLs with real society images in the DB.
- [ ] Insert real IBAN/TRR in `public/includes/donations.php`.
- [ ] Embed the official CNVOS iframe for income tax donation.
- [ ] Update `CONTACT_EMAIL` in `public/api/config/config.php`.

---
*Generated by Gemini CLI - Visual & Logic Refinement Phase.*
