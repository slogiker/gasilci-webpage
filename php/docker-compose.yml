version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./data:/var/www/html/data
      - ./public/api/uploads:/var/www/html/public/api/uploads
    restart: unless-stopped
